==display :'utils/flash'
h2 Update email
form action="/settings/email" method="POST"
	input type="email" name="email" placeholder="Email" maxlength="254" value="#{@email}"
	input type="submit" value="Update email"

h2 Update password
form action="/settings/password" method="POST"
	input type="password" name="old_password" placeholder="Old password" maxlength="64" required=""
	input type="password" name="new_password" placeholder="New password" maxlength="64" required=""
	input type="password" name="repeat_password" placeholder="Repeat password" maxlength="64" required=""
	input type="submit" value="Update password"